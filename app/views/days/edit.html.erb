<%= simple_form_for [ @shop, @day ] do |f| %>
  <%= f.input :name %>
  <%= f.input :closed %>
  <!-- s'il y a des horaire, on les affiche chaque horaire avec edit et destroy
  ou un boutton ajouter un horaire -->
  <%= f.submit "Submit jour", class: "btn btn-primary" %>
<% end %>


